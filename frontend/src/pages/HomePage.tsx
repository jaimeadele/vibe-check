import Layout from '../components/Layout';

export default function HomePage() {
  return (
    <Layout subtitle='Find a live event near you'>
      <p className='text-gray-500 text-sm'>Operator directory — coming in Step 6</p>
    </Layout>
  );
}
